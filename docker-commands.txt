# Docker Commands - may-backend

## Build
docker build -t ozanemrahyakupoglu/may-backend:1.0.4 .

## Push (Docker Hub)
docker push ozanemrahyakupoglu/may-backend:1.0.4

## Run
docker run -d \
  --name may-backend \
  -p 8090:8090 \
  -e DB_HOST=host.docker.internal \
  -e DB_USER=MAY_APP \
  -e DB_PASSWORD=MAY_APP \
  ozanemrahyakupoglu/may-backend:1.0.4

---

# Docker Compose Commands - may-backend

## Build
docker compose build

## Build (with version)
VERSION=1.0.6 docker compose build

## Push (Docker Hub)
docker compose push

## Push (with version)
VERSION=1.0.6 docker compose push

## Run
docker compose up -d

## Run (with version)
VERSION=1.0.6 docker compose up -d
